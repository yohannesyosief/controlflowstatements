package ArrayListJavaTPoint;
//Java ArrayList-
//Java ArrayList use a dynamic array for storing the elements.It is like an array,but there is no size limit.We can add or remove
//elements anytime.So is much more flexible than the traditional array.found in the java.util package
//Can have duplicate elements.it extends List Interface,it maintains the insertion internally
//ArrayList extends AbstractList implements List extends Collection extends iterable.
//Important points about Java ArrayList are:
//Java ArrayList class can contain duplicate elements
//Maintains insertion order,is non Synchronized,allows random access because array works at the index basis,
//ArayList manipulation is little bit slower than the LinkedList in Java because a lot of shifting needs to occur if any element is
//removed from the array list
//public class ArrayList<E>extends AbstractList<E>implements List<E>,RandomAccess,Cloneable,Serializable
//Creating old generic arraylist--> ArryList list=new ArrayList()
//Creating new generic arraylist-->ArrayList<String> list=new ArrayList<String>();

public class BasicCOncepts {

}
