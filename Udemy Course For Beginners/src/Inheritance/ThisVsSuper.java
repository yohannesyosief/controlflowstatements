package Inheritance;
//The super keyword is used to access/call the parent class members(variables and methods).
//The keyword This is used to call the current class members(variables and methods).This is required
//when we have a parameter with the same as an instance variable(field)
//Note: we can use both of them anywhere in a class except static areas(The static block or a static method).Any attempt to do so will lead to compile-time errors
//(more on static later in the course)
public class ThisVsSuper {

}
